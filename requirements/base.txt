# Inference
--extra-index-url https://wheels.vllm.ai/nightly
vllm
lmcache==0.4.2         # KV-cache offload layer
scipy>=1.12
scikit-learn>=1.5
pandas>=2.1
numba>=0.60

# Gateway / proxy
litellm[proxy]==1.86.0

# Config + runtime
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27             # async health checks + load tests
python-dotenv>=1.0
psutil>=5.9              # port/process introspection for tunnel up port-adoption

# Observability
structlog>=24.0
prometheus-client>=0.20