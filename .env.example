# Copy to .env — never commit .env

# HuggingFace token (required for gated models)
HF_TOKEN=hf_...

# Override LiteLLM master key from models.yaml
LITELLM_MASTER_KEY=sk-tunnel-change-this

# LMCache Redis (only if backend=redis)
LMCACHE_REDIS_HOST=localhost
LMCACHE_REDIS_PORT=6379

# Pin GPU visibility for multi-GPU setups
CUDA_VISIBLE_DEVICES=0,1